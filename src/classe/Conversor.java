package classe;

//INTERFACE PARA CONVERSÃO
public interface Conversor {
 double celsiusParaFahrenheit(double celsius);
 double quilometrosParaMilhas(double quilometros);
 double gramasParaLibras(double gramas);
}